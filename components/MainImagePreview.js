import React from "react";
import PropTypes from "prop-types";

function MainImagePreview({ value }) {
  return <div>MainImagePreview</div>;
}

MainImagePreview.propTypes = {
  value: PropTypes.node,
};

export default MainImagePreview;
